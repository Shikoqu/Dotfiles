#!/usr/bin/env zsh

# 1. Base Environment Configurations & Paths
zsh-users/zsh-completions kind:fpath path:src
junegunn/fzf kind:clone
junegunn/fzf path:bin kind:path

# 2. Completion Initialization (Must happen BEFORE fzf-tab)
mattmc3/ez-compinit
belak/zsh-utils path:completion/functions kind:autoload post:compstyle_zshzoo_setup

# 3. FZF-TAB (Must be right after compinit, but BEFORE syntax/autosuggestions)
aloxaf/fzf-tab

# 4. Utilities & Settings
belak/zsh-utils path:editor
belak/zsh-utils path:history
belak/zsh-utils path:utility
zshzoo/macos conditional:is-macos
romkatv/zsh-bench kind:path
ohmyzsh/ohmyzsh path:plugins/extract

# 5. Fish-like features & Interceptors (Must load AFTER fzf-tab)
zdharma-continuum/fast-syntax-highlighting
zsh-users/zsh-autosuggestions
zsh-users/zsh-history-substring-search

# 6. Prompt Theme
romkatv/powerlevel10k

# 7. fzf ???
# junegunn/fzf kind:clone
# junegunn/fzf path:shell kind:zsh
# junegunn/fzf path:bin kind:path

# 8. zoxide & eza ???
# z-shell/zsh-eza # install via HomeBrew
# ajeetdsouza/zoxide # better to provide `--cmd cd` to ` eval "$(zoxide init zsh --cmd cd)"`
